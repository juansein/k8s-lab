version: "3.9"

services:
  backend:
    build: ./backend
    ports:
      - "8081:8000"
    container_name: backend

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    container_name: frontend
    depends_on:
      - backend
